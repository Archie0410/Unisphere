# ===========================================
# UniSphere - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values
# cp env.example .env

# ===========================================
# ESSENTIAL VARIABLES (Must Configure)
# ===========================================

# Server Configuration
NODE_ENV=development
PORT=5000
# Note: On Render, PORT is automatically provided. Use process.env.PORT in your code.
API_VERSION=v1

# Database Configuration (MongoDB)
# Local MongoDB
# MONGODB_URI=mongodb://localhost:27017/edupath_ai
# MongoDB Atlas (Cloud)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/edupath_ai?retryWrites=true&w=majority

# JWT Configuration (Critical for authentication)
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=7d
JWT_REFRESH_SECRET=your_refresh_secret_here
JWT_REFRESH_EXPIRES_IN=30d

# AI Service (Google Gemini - Primary AI service)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# Security
CORS_ORIGIN=http://localhost:3000
# For Render: Add your frontend URL(s) separated by commas
# Example: CORS_ORIGIN=https://your-frontend.onrender.com,https://your-frontend.vercel.app
# Note: RENDER_EXTERNAL_URL is automatically provided by Render
# Generate a secure secret: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your_session_secret_here

# ===========================================
# RECOMMENDED VARIABLES (Configure for full functionality)
# ===========================================

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging
LOG_LEVEL=info
LOG_FILE_PATH=./logs

# Email Configuration (Gmail)
# Enable 2FA on your Gmail account and generate an App Password
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_app_password_here
EMAIL_FROM=UniSphere <noreply@unisphere.ai>

# ===========================================
# OPTIONAL VARIABLES (Advanced features)
# ===========================================

# Redis (for caching and session storage)
REDIS_URL=redis://localhost:6379
REDIS_PASSWORD=

# External APIs
UNIVERSITY_API_KEY=your_university_api_key
GOOGLE_MAPS_API_KEY=your_google_maps_api_key


# OpenAI (Alternative AI service)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4

# SMS (Twilio)
# Get your credentials from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890

# File Upload Configuration
UPLOAD_PATH=./uploads
MAX_FILE_SIZE=5242880
ALLOWED_FILE_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Monitoring (Sentry)
SENTRY_DSN=your_sentry_dsn

# ===========================================
# QUICK START GUIDE
# ===========================================
# 1. Copy this file to .env: cp env.example .env
# 2. Generate a secure JWT_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# 3. Get a Gemini API key from Google AI Studio
# 4. Set up PostgreSQL database (optional for demo mode)
# 5. Configure email settings (optional)
# 6. Start the server: npm run dev
